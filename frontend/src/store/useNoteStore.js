import { create } from 'zustand';

const useNoteStore=create((set,get)=>({
    notes:[],
    getAllNotes:async ()=>{
        try{

        }
        catch(error){
            
        }
    }
}));

export default useNoteStore;